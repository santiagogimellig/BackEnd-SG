<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container">
        <a class="navbar-brand" href="/">Santa Tabla</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ml-auto">
                {{#if user}}
                <li class="nav-item">
                    <a class="nav-link" href="#">¡Hola, {{user.first_name}}!</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link btn btn-danger" href="/api/sessions/logout">Cerrar sesión</a>
                </li>
                {{else}}
                <li class="nav-item">
                    <a class="nav-link" href="/">Iniciar sesión</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link btn btn-success" href="/register">Registrarse</a>
                </li>
                {{/if}}
            </ul>
        </div>
    </div>
</nav>